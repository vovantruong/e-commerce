import React from 'react'
import classNames from 'classnames/bind'
import styles from './WhyChose.module.scss'

const cx = classNames.bind(styles)

const WhyChose = () => {
  return(
    <div className={cx('why-chose-area')}>
        WhyChose
    </div>
  )
}
export default WhyChose